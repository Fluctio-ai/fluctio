// Package setup — web gateway request handlers.
//
// Single-user mode: the public discovery endpoints (GET /api/public/agents
// "Agent 广场" and GET /api/public/skills "技能库") and link-sharing via
// agents.is_public have been removed. The platform serves one owner; agents
// are not published to a public marketplace.
package setup
