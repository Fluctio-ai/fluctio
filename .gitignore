# Binaries
/fluctio
*.exe
*.exe~
*.dll
*.so
*.dylib

# Build output
/bin/
/dist/

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out
*.prof

# Go workspace file
go.work
go.work.sum

# Dependency directories
vendor/
node_modules/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Environment and secrets
.env
.env.*
!.env.example
*.pem
*.key

# Config with secrets (users create their own)
config.json
fluctio.json

# Debug
debug/
__debug_bin*
DESIGN.md

# Air hot reload
tmp/
.air.toml

# Deploy instance data (tokens, passwords, local state)
deploy/docker/instances/
deploy/docker/.env
deploy/docker/.state/

# Web build artifacts (embedded at build time)
internal/setup/web/
web/out/
web/.next/

openclaw.json

# Runtime config
env.toml

# Local data
web/node_modules/
web/pnpm-lock.yaml
fluctio-patched

# Playwright MCP tooling artifacts (local only)
.playwright-mcp/

# Local gateway run logs (stdout/stderr captures)
run.log
run.log.*
run.err
run.err.*
gw.err
gw.err.*

# runtime logs (restart*.log)
*.log

# Local-only SVG asset (wave animation icon, not for repo)
/ic_动画_动作_波浪.svg

# Local agent outputs (generated files, not for repo)
/outputs/

# Local-only docs (references/notes, not shipped)
/docs/

# Local specs/plans (private design notes, not for repo)
/specs/

# Private local CLI (self-use, not for repo)
/cmd/ga-hook/
/cmd/daka-hook/

# Local cross-compiled binaries for deployment testing
/fluctio-linux
/ga-hook-linux
/daka-hook-linux

# Local superpowers agent/worktree docs (private, not for repo)
/.superpowers/
